/* global jest, it, describe, expect */
import store from '@/store/index'

// jest.mock('@/store/callZome')

describe('holochain store', () => {
  it('indicates that an agent handle is needed whenever no storedAgentHandle is found', async () => {})

  it('sets agent handle', async () => {})

  it('edits handle', async () => {})

  it('clears handle', async () => {})
})
